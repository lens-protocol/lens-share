export enum PlatformType {
  Web = "web",
  Mobile = "mobile",
}

export enum RouteKind {
  Home = "home",
  Profile = "profile",
  Publication = "publication",
}

export enum SelectionMode {
  JustOnce = "justOnce",
  Always = "always",
}
